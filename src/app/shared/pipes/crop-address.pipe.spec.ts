import { CropAddressPipe } from './crop-address.pipe';

describe('CropAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new CropAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
